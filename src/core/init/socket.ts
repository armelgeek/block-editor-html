//import io from 'socket.io-client';

const getSocket = (userId:any) => {
  /**
  const socket = io('http://localhost:8100', {
    query: { userId },
    transports: ['websocket'],
  });

  return socket; */
  return null;
};

export default getSocket;